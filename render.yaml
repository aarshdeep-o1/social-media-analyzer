services:
  - type: web
    name: social-analytics-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run social-analytics-dashboard/app.py --server.port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
